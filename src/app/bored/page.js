import BoredWithPendulum from '@/components/bored/BoredWithPendulum';

export const metadata = {
  title: 'AntiBoredom — Raf</>Console Studio',
  description: 'Game modes, AIM training and an interactive double-pendulum observation scene.',
};

export default function BoredPage() {
  return <BoredWithPendulum locale="en" />;
}
